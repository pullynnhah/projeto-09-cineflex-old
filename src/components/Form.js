import styled from "styled-components";

function BuyerInput() {
  return (
    <>
      <Label></Label>
    <Input />

    </>
  )
}

export default function BuyerForm({}) {
  return (
    <Form onSubmit={}>

    </Form>
  );
}

const Form = styled.form``;